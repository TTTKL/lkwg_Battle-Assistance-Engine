# 下一步方向

更新日期：2026-04-11

## 当前判断

现在最值得继续做的，不是再铺更大的功能面，而是把下面三件事继续打稳：

1. 明箱 / 暗箱搜索结果的稳定性与可信度
2. `engine_core/agent_runtime` 的状态与回放闭环
3. 规则尾项与文档口径的一致性

## 高优先级

### 1. 继续校准明箱搜索与胜率

- ~~检查更深搜索下，行动排序和截断是否仍引入系统性偏差。~~ （已修复：同时博弈 Maximin）
- ~~评估是否需要把部分局面从纯策略近似进一步推进到混合策略求值。~~ （已实现：期望值搜索）
- ~~搜索树中 random.random() 导致非确定性。~~ （已修复：同速确定性打破平局 + 50回合按局面判定）
- ~~评估器心数估值方向反了、速度评估不对称。~~ （已修复至 v3）
- ~~/simulate 对手视角翻转不完整。~~ （已重写：正确的对手 minimax 搜索）
- ~~无胜率估算。~~ （已新增：sigmoid 胜率映射 + 全行动评分 + 多局模拟统计）
- 做更多镜像局面、对称局面、满能量 / 同速 / 同技能局面的回归测试
- 验证伤害公式精度：当前缺少等级因子和随机浮动（文档中已说明为确定性近似）

### 2. 继续打磨暗箱预判系统

- 对手行为预测器已具备三层模型（先验/局面/历史），下一步：
  - 补充更多局面特征（对手特性推断、技能属性覆盖、能耗可行性过滤等）
  - 用实机对局数据校准先验参数（当前为经验值）
  - 将 BehaviorPredictor 的 `record_from_event()` 接入 SessionManager 的事件流水线
- 验证预读分析在不同局面下的建议质量

### 3. 为最近修过的规则补针对性验证

- 聚能在满能量下仍可选。
- 聚能可被 `应对状态` 正常应对。
- 非法换宠不会再被推荐。
- 推荐换宠时会明确指出换哪一只。
- 推荐高亮与选中高亮不会互相覆盖。
- 传动后技能位顺序、实时能耗、冷却在前端展示 / 分析请求 / 引擎返回之间保持一致。
- 圣剑-X 等依赖槽位或改耗的用例，继续补前端同步回归验证。
- 新补完的萌化退化种族回退、雷暴整段复放、槽位条件通用 buff/debuff 继续加回归测试。

## 中优先级

### 4. 继续加固 `engine_core/` 状态底盘

- 补 `PetInstance / TeamState / BattleState` 字段级一致性测试。
- 继续把高风险运行时状态从 `pet._xxx` 收口到显式字段或统一容器。
- 降低 `trait_processor` 中旧式动态字段写法的占比。

### 5. 继续推进 `agent_runtime`

- 增加 replay 一致性测试。
- 验证 import / rollback / clear / append event 后状态是否一致。
- 继续明确 observation、inference、assembled state 的边界。

### 6. 规则尾项收口

- 继续收口更复杂的槽位 / 传动非伤害衍生效果。
- 持续同步 `README.md`、`implementation_progress.md`、`unimplementable_mechanics.md`、`unimplementable_traits.md`。
- 对技能 / 特性 / 特殊机制补 exact / approximate / stub 标记，让调试输出能直接暴露近似实现。

### 7. 旧网页同步链继续减风险

- 继续排查 `index.html` 中“静态技能数据覆盖实时状态”的残留读取点。
- 继续排查前端残余静态读取点，确保动作合法性、技能顺序、能耗和冷却都优先读取引擎返回状态。
- 对技能位、能耗、冷却、换宠后状态刷新补更直接的前端回归用例。

## 低优先级

### 8. 更进一步的推荐解释能力

- 在旧网页或 `agent_runtime` 中补更明确的：
  - 风险提示
  - 置信度
  - 当前依赖的关键假设

### 9. 更激进的搜索或自动化能力

- 更复杂的不确定性搜索
- OCR / 屏幕识别
- 更自动的日志采集
